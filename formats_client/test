python -c "
import pandas as pd
xl = r'T:\Dallas\CSS-BOC\CTL\REC\Réconciliation dérivés listés\ASB AXION\2026\01. JANVIER\PTF_AXB_AXI_SICAV_31_01_2026.xlsx'
xf = pd.ExcelFile(xl)
for sheet in xf.sheet_names:
    df = pd.read_excel(xf, sheet_name=sheet, header=None)
    row3 = df.iloc[3]  # ligne 4 index 3
    print(f'=== {sheet} ===')
    for i, v in enumerate(row3):
        if str(v).strip() not in ['', 'nan']:
            print(f'  col {i} ({chr(65+i)}): {repr(v)}')
"
