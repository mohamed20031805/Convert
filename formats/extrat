python -c "
from formats import morgan
lignes = morgan.extraire_positions('pdfs/Futures and Options NY customer Statements - 052BATEQ7 - 30Jan2026-4.pdf')
print(f'Lignes extraites : {len(lignes)}')
if lignes:
    print(lignes[0])
"


python -c "
import pdfplumber
p = 'pdfs/Futures and Options NY customer Statements - 052BATEQ7 - 30Jan2026-4.pdf'
with pdfplumber.open(p) as pdf:
    for i, page in enumerate(pdf.pages[:3]):
        print(f'--- PAGE {i+1} ---')
        texte = page.extract_text()
        if texte:
            for j, ligne in enumerate(texte.split('\n')[:25]):
                print(f'[{j}] {ligne}')
        print()
"
