python -c "
import pdfplumber
import re

p = r'T:\Dallas\CSS-BOC\CTL\REC\Réconciliation dérivés listés\OFI\2026\01. JANVIER\20260130_DLY_STMT.pdf'
with pdfplumber.open(p) as pdf:
    for i, page in enumerate(pdf.pages[:2]):
        texte = page.extract_text() or ''
        print(f'--- PAGE {i+1} ---')
        print('POSITIONS OUVERTES in texte:', 'POSITIONS OUVERTES' in texte)
        print('ACHAT in texte:', 'ACHAT' in texte)
        print('VENTE in texte:', 'VENTE' in texte)
        print()
        for ligne in texte.split('\n'):
            if any(x in ligne for x in ['ACHAT','VENTE','POSITION','AVG','22MAY']):
                print(repr(ligne))
"
"
