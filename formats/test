python -c "
import pdfplumber
p = r'T:\Dallas\CSS-BOC\CTL\REC\Réconciliation dérivés listés\FUND CHANNEL\2026\BAML\MSIM_SG_EOD Daily Statement.pdf'
with pdfplumber.open(p) as pdf:
    page = pdf.pages[1]
    texte = page.extract_text()
    lignes = texte.split('\n')
    for ligne in lignes:
        if 'LONG' in ligne and 'SHORT' in ligne and 'CONTRACT' in ligne:
            print('HEADER:')
            print(ligne)
            print('0123456789' * 12)
            for i in range(0, len(ligne), 10):
                print(str(i).ljust(10), end='')
            print()
        if '12/05' in ligne or '27*' in ligne:
            print('DATA:')
            print(ligne)
            print('0123456789' * 12)
            for i in range(0, len(ligne), 10):
                print(str(i).ljust(10), end='')
            print()
"
